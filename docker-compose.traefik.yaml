version: "3.8"
# commands url http://host.docker.internal:6777
services:
  wds-blogs-traefik-app:
    image: "ghassanaldarwish/wds-blogs"
    restart: on-failure
    networks:
      - app-workspace-network
      - frontend
    environment:
      - IS_DOCKER=true
      - CHOKIDAR_USEPOLLING=true
      - WDS_SOCKET_PORT=0
      - NODE_ENV=production
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.wds-blogs-traefik-app.rule=Host(`blogs.wdsui.com`)"
      - "traefik.http.routers.wds-blogs-traefik-app.entrypoints=websecure"
      - "traefik.http.routers.wds-blogs-traefik-app.tls.certresolver=letsencrypt"
      - traefik.http.routers.wds-blogs-traefik-app.tls=true
      - "traefik.http.services.wds-blogs-traefik-app.loadbalancer.server.port=3000"
    # env_file:
    #   - ../environment/wds-workspace/.env.local

networks:
  app-workspace-network:
    name: app-workspace-network

  frontend:
    name: frontend
